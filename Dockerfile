# Используем официальный образ Python
FROM python:3.13

# Устанавливаем рабочую папку
WORKDIR /app

# Копируем все файлы проекта
COPY . .

# Команда по умолчанию (будем переопределять при запуске)
CMD ["python", "main.py"]